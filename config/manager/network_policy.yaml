apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-all-ports
  namespace: default
spec:
  podSelector: {}
  ingress:
    - ports:
        - protocol: TCP
          port: 1-65535
  policyTypes:
    - Egress
